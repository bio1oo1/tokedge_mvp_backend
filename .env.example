# Firebase (service account for Admin SDK / Data Connect)
# Use a service account JSON or set individual fields:
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk@your-project.iam.gserviceaccount.com

# Firebase Data Connect (Firebase Postgres)
DATA_CONNECT_SERVICE_ID=tokedge-access
DATA_CONNECT_LOCATION=us-central1
DATA_CONNECT_CONNECTOR=default

# Local testing (no Blaze): set this to use the Data Connect emulator
# Start emulator first: yarn emulator:dataconnect
DATA_CONNECT_EMULATOR_HOST=127.0.0.1:9399

# External APIs
NANSEN_API_KEY=your-nansen-api-key

# Application
PORT=3000
ALLOWED_ORIGINS=http://localhost:3000,https://access.tok-edge.com
